import os
from setuptools import setup, find_packages
import testapp


setup(name='testapp',
      version='0.0',
      description='Django Test App',
      author='test',
      url='',
      packages=find_packages(),
     )